trigger: none

pool:
  vmImage: 'Ubuntu-16.04'

steps:
- task: Bash@3
  inputs:
    targetType: 'inline'
    script: 'export DOCKER_PASSWORD=$(docker_password)
      export DOCKER_USERNAME=$(docker_username)'
- script: 'build.sh'
  workingDirectory: 'kafka-manager/build'
- script: 'push.sh'
  workingDirectory: 'kafka-manager/build'

