trigger:
  branches:
    include:
    - master
  paths:
    include:
    - zookeeper-rest/build/*

pool:
  vmImage: 'Ubuntu-16.04'

steps:
- bash: |
    cd $(Build.SourcesDirectory)/zookeeper-rest/build
    ./build.sh
- bash: |
    export DOCKER_PASSWORD=$(docker_password)
    export DOCKER_USERNAME=$(docker_username)
    cd $(Build.SourcesDirectory)/zookeeper-rest/build
    ./push.sh

